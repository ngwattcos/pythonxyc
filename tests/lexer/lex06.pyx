
#@let a = -1
#negative numbers are broken
#| Minus of aexp * aexp

#@let r = True == True
#equality for booleans are broken
#| NE of aexp * aexp
#| EQ of aexp * aexp
#really should be | NE of bexp * bexp